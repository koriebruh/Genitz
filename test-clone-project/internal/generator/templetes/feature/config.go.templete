package config

import "os"

type Config struct {
	AppName string

{{range .ConfigFields}}
{{.}}
{{end}}
}

func LoadConfig() Config {
	return Config{
		AppName: os.Getenv("APP_NAME"),

		{{range .ConfigInit}}
		{{.}}
		{{end}}
	}
}