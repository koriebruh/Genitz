package generator

// Version is Genitz's own release version, bumped by hand per release —
// there's no goreleaser/ldflags pipeline here yet, so this is the single
// source of truth for `genitz version`/`--version`.
const Version = "0.1.0"
