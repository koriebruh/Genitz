version: '3.8'

services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - APP_ENV=local
    # postgres:
    #   image: postgres:15-alpine
    #   ports:
    #     - "5432:5432"
    #   environment:
    #     POSTGRES_USER: user
    #     POSTGRES_PASSWORD: password
    #     POSTGRES_DB: {{.ProjectName}}_db
